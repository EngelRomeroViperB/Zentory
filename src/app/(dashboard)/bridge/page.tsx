import { PrintBridge } from "@/components/pos/PrintBridge";

export default function BridgePage() {
  return (
    <div className="py-10 bg-slate-100 min-h-[calc(100vh-4rem)]">
      <PrintBridge />
    </div>
  );
}
